apply plugin: 'java-test-fixtures'

dependencies {
    api libs.reactor.core
    api libs.slf4j.api

    // @ScaleTest 가 JUnit 의 @Tag 를 담는다. 버전은 Boot BOM 이 정한다.
    testFixturesApi 'org.junit.jupiter:junit-jupiter-api'
}
